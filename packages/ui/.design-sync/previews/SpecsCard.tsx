import { SpecsCard } from '@arf/ui';

export function Default() {
  return (
    <SpecsCard
      specs={['Core Governance Engine', 'API Control Plane', 'Enterprise Layer', 'Enterprise Specification']}
      href="/signup"
    />
  );
}
